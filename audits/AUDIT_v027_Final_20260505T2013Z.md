# AUDIT — v0.27.0 Final (Cumulative Ship-Gate)
**Date:** 2026-05-05T20:13Z  
**Auditor:** W-Audit-v027-Final, one-shot Sonnet CLI worker  
**Scope:** 7 workers post-0.26.0: Carryover Polish Gaps 1+2, W-QA-Harness-Mobile-Ext, W-Event-System-Scaffold, W-Tutorial-Content-Pass, W-Content-Expansion-Stages-19-24, W-Tower-Art-Procedural, W-i18n-Scaffolding  
**Type:** Read-only structural audit — ZERO code changes  
**Predecessor:** AUDIT_Final_Launch_20260505T1830Z (SHIP-READY for 0.26.0)

---

## §0 OVERALL VERDICT: SHIP-WITH-FIXES

All 7 workers pass §A (syntax) and §F (failure modes). Code quality is sufficient for submission. **One structural fix required before shipping:** workers 3-7 have all code changes uncommitted — done markers are untracked files. The Architect must commit the working tree (STATE_OF_BUILD.md, index.html, js/hunt/hunt-bosses.js, js/hunt/hunt-enemies.js, js/hunt/hunt-render.js, js/hunt/hunt-stage.js, js/meta/meta-missions.js, js/wg-game.js, plus all untracked new files) before any app-store submission.

Additionally, two §C catalog discipline violations surface in the new ascended-biome boss mechanics — non-blocking but should be carried as polish.

**Required before ship (Architect action):**
- Commit workers 3-7: 12 modified/new files + 5 untracked done markers

**Polish gaps (non-blocking, carry forward):**
1. meta-daily-rewards.js flyIcon cancel-guard still missing (partial fix; hunt-results.js was fixed by carryover commit)
2. `fe.hp = fe.maxHp = 120` hardcoded in `hunt-bosses.js tickBoss()` — should be `td.splitFragmentHp`
3. memory_husk split type/count hardcoded in `wg-game.js` enemy:killed handler — should reference enemy catalog
4. MutationObserver `_buffObs` in `hunt-tutorial-ext.js` never disconnected (no-ops after completion; harmless)
5. `_bannerEl` in `meta-ltd-events.js` can reference a detached DOM node between `showHuntStageList()` calls

**Architect-gated items (unchanged from 0.26.0):**
All prior items (legal entity name, AdMob IDs, StoreKit wiring, code signing, ASC record) remain outstanding and unchanged — see 0.26.0 audit §J.

---

## §A — Syntax (node --check on all modified/new .js files)

Files checked directly in this session:

| File | Type | Result |
|------|------|--------|
| js/core/wg-i18n.js | NEW | **PASS** |
| js/meta/meta-ltd-events.js | NEW | **PASS** |
| js/meta/meta-missions.js | MOD | **PASS** |
| js/hunt/hunt-tutorial-ext.js | NEW | **PASS** |
| js/hunt/hunt-tower-render.js | NEW | **PASS** |
| js/hunt/hunt-render.js | MOD | **PASS** |
| js/hunt/hunt-enemies.js | MOD | **PASS** |
| js/hunt/hunt-bosses.js | MOD | **PASS** |
| js/hunt/hunt-stage.js | MOD | **PASS** |
| js/wg-game.js | MOD | **PASS** |
| js/qa/qa-harness.js | MOD | **PASS** |

**§A cumulative: ALL PASS** — zero syntax errors across all 11 modified/new JS files.

---

## Per-worker §A–§J matrix

### 1. Carryover Polish Gaps 1+2 (commit db8934c)

| § | Item | Verdict | Evidence |
|---|------|---------|----------|
| A | Syntax | **PASS** | node --check clean: js/core/wg-cache.js, js/hunt/hunt-results.js |
| B | Marker integrity | **PASS** | Single commit db8934c. No done marker required for inline carryover commits. |
| C | Tunable discipline | N/A | No mechanics tunables changed. |
| D | Performance | **CONDITIONAL PASS** | hunt-results.js flyIcon: rafId + cancelled flag added; `if (cancelled \|\| !el.parentNode) { cancelAnimationFrame(rafId); return; }` guard correct. **meta-daily-rewards.js flyIcon still lacks cancel-guard** — prior audit gap 1 was only half-resolved. Carried forward as Polish Gap 1 below. |
| E | Build-on | **PASS** | wg-cache.js + hunt-results.js only. hunt-enemies.js, hunt-bosses.js, hunt-stage.js, hunt-waves.js, hunt-player.js untouched. |
| F | Failure modes | **PASS** | `s.forge.rareMaterials = data.forge.rareMaterials \|\| s.forge.rareMaterials \|\| 0` — null/zero handled. flyIcon cancel-guard has `!el.parentNode` guard as fallback. |
| G | Path A | N/A | |
| H | App Store | N/A | |

**Worker verdict: PASS** *(with Polish Gap 1 carried: meta-daily-rewards.js flyIcon still missing cancel-guard)*

---

### 2. W-QA-Harness-Mobile-Ext (commits abc19b4, 848af6e, 697536b)

| § | Item | Verdict | Evidence |
|---|------|---------|----------|
| A | Syntax | **PASS** | node --check clean: js/qa/qa-harness.js |
| B | Marker integrity | **PASS** | Done marker committed at 697536b. 3 concern commits (A+B abc19b4, C 848af6e) + output-doc commit 697536b. SCENARIOS array verified: 13 total (8 original + 5 new mobile). |
| C | Tunable discipline | N/A | Dev/QA tool. No mechanics constants. |
| D | Performance | **PASS** | New scenarios use only `dispatchEvent`, `setTimeout`-based wait helpers, and synchronous DOM checks. No rAF, no setInterval. |
| E | Build-on | **PASS** | Only qa-harness.js (modified) and docs/QA_HARNESS.md modified. No game JS files touched. |
| F | Failure modes | **PASS** | Scenarios are idempotent; stub missing Capacitor plugins (`WG.Haptics`, `ionBackButton`); fallback paths documented. `back_button_navigation` dispatches `CustomEvent('ionBackButton')` and verifies no console errors — correct no-op baseline. |
| G | Path A | N/A | Dev tool; not exposed in prod UI. |
| H | App Store | **PASS** | QA_HARNESS.md updated with 5 new scenario rows + "Mobile-specific notes" section + browser-vs-native fidelity table. |

**Worker verdict: PASS**

---

### 3. W-Event-System-Scaffold

| § | Item | Verdict | Evidence |
|---|------|---------|----------|
| A | Syntax | **PASS** | node --check clean: meta-ltd-events.js, meta-missions.js |
| B | Marker integrity | **FAIL** | Done marker (`workers/done/W-Event-System-Scaffold.done`) is UNTRACKED — not committed. Code changes are in working tree only. No READY_FOR_AUDIT.md found in WORKER_OUTPUT dir. Evidence is via worker stdout.log and git diff only. |
| C | Tunable discipline | **PASS** | `CATALOG = Object.freeze([...])` at meta-ltd-events.js:6. All 3 event definitions in frozen array. `eventBuffs` in WG.State is a dynamic runtime store (not a mechanics tunables catalog) — acceptable. |
| D | Performance | **CONDITIONAL PASS** | `setInterval(fn, 3600000)` in `init()` has no double-init guard. In practice `WG.Game.init()` runs once and `LtdEvents.init()` is called once from it — low risk. `_bannerEl` can reference a detached DOM element between `showHuntStageList()` calls; `_refreshBanner` modifies detached DOM harmlessly (Polish Gap 5). Banner event listeners are on dynamically created elements — no persistent listener pile-up. |
| E | Build-on | **PASS** | meta-ltd-events.js (new), meta-missions.js (modified), wg-game.js (evtBannerSlot + init chain). hunt-enemies.js, hunt-bosses.js, hunt-stage.js, hunt-waves.js, hunt-player.js UNTOUCHED by this worker. |
| F | Failure modes | **PASS** | `_bannerEl` null-guard in `_refreshBanner()`. `activeEvents()` returns [] when no events match — UI hides gracefully. `WG.Missions.setEventMissions([])` handled by `clearEventMissions()`. Mission `claim()` extension has proper `if (!mDef \|\| !tracker) return false` guard. |
| G | Path A | N/A | Event buffs are gameplay modifiers, not monetization paths. |
| H | App Store | N/A | |
| J | Events deferral | **PASS** | Module comment: "Server scheduling = Phase 4 — dates are hardcoded." `_todayStr()` uses `WG.MetaDailyReset.todayStr()` (falls back to `new Date().toISOString().slice(0,10)`). All three events have hardcoded ISO date ranges. Client-side date comparison works. |

**Worker verdict: CONDITIONAL PASS** *(§B marker integrity fail — done marker uncommitted)*

---

### 4. W-Tutorial-Content-Pass

| § | Item | Verdict | Evidence |
|---|------|---------|----------|
| A | Syntax | **PASS** | node --check clean: js/hunt/hunt-tutorial-ext.js |
| B | Marker integrity | **FAIL** | Done marker UNTRACKED. |
| C | Tunable discipline | N/A | No mechanics constants. |
| D | Performance | **CONDITIONAL PASS** | MutationObserver `_buffObs` (`{ childList: true }` on document.body) never disconnected after tutorial completes. Callbacks gate on `isDone('towerTutorial')` — effectively no-op post-completion. `if (_buffObs) return` guard prevents double-subscription. Observer is lightweight and won't prevent GC of DOM elements. Polish Gap 4. |
| E | Build-on | **PASS** | hunt-tutorial-ext.js (new), index.html (script tag), wg-game.js (init call + settings section). hunt-enemies.js, hunt-bosses.js, hunt-stage.js, hunt-waves.js, hunt-player.js UNTOUCHED. |
| F | Failure modes | **PASS** | All hint `showHint()` calls have `isDone()` guards. Tower sequence: intro → buff-picker (MutationObserver, 300ms defer, re-checks buff picker still exists) → death via `tower:run-end`. Forge: `getBoundingClientRect()` used for positioning (hint suppressed if forge tab not visible). Gacha: `eligibility check (free summon \|\| gems ≥ 30 \|\| stage ≥ 3)` before showing. `resetAll()` correctly re-arms all flags + dismisses active hints. |
| G | Path A | N/A | Tutorial UX; no monetization paths. |
| H | App Store | **PASS** | RESET TUTORIAL button wired in Settings modal above SAVE DATA section. |

**Worker verdict: CONDITIONAL PASS** *(§B marker uncommitted; §D MutationObserver non-disconnect — non-blocking)*

---

### 5. W-Content-Expansion-Stages-19-24 (worker name: Pale)

| § | Item | Verdict | Evidence |
|---|------|---------|----------|
| A | Syntax | **PASS** | node --check clean: hunt-enemies.js, hunt-bosses.js, hunt-stage.js, hunt-render.js, wg-game.js |
| B | Marker integrity | **FAIL** | Done marker UNTRACKED. |
| C | Tunable discipline | **CONDITIONAL PASS** | Enemy catalog and boss catalog frozen properly. **Two violations in engine code:** (1) `fe.hp = fe.maxHp = 120` hardcoded in `tickBoss()` (hunt-bosses.js:146) — should be `td.splitFragmentHp || 120` with `splitFragmentHp: 120` in `echo_throne_keeper` catalog entry. (2) memory_husk split type (`'lurker'`) and count (`2`) hardcoded in wg-game.js `enemy:killed` handler — should live in enemy catalog entry. Both are Polish Gaps 2+3 — non-blocking because they're single-call mechanics values with no runtime variance. |
| D | Performance | **PASS** | Fragment split is a one-shot event (guarded by `!b._splitDone`). No rAF, no setInterval in new code. New enemy types (sigil_drone, memory_husk) use existing draw pipeline; no new animation loops. Hunt-render.js `_drawFrameTower()` is a render path, not a parallel loop. |
| E | Build-on | **CONDITIONAL PASS** | hunt-enemies.js, hunt-bosses.js, hunt-stage.js modified — this is by design for content expansion. Verified via git diff: (a) hunt-enemies.js: 2 new entries appended after existing enemies; all existing entries unchanged; (b) hunt-bosses.js: 2 new catalog entries; fragment-split code added in `tickBoss()` after existing tick paths without modifying them; (c) hunt-stage.js: `ascended` added to BIOMES; 6 stages appended after stage 18 array entry; post-loop stage-24 override added after existing loop. **Existing stages 1-18, enemies 1-11, and bosses 1-6 confirmed UNTOUCHED.** hunt-waves.js, hunt-player.js UNTOUCHED. |
| F | Failure modes | **PASS** | Fragment split: `!b._splitDone` one-shot guard. Boss immunity: `!(hit._fragmentsAlive > 0)` tested before damage. Fragment death: `Math.max(0, boss._fragmentsAlive - 1)` prevents negative. Stage 24 waveCount override: `STAGES.find(s => s.id === 24)` null-checked before assignment. |
| G | Path A | **PASS** | Ascended content is locked behind stage 18 completion. No monetization changes. |
| H | Stage 19-24 lock | **PASS** | `isStageUnlocked(stage, allStages)` uses `prev = allStages.find(s => s.id === stage.id - 1)` → `bestWaves[prev.id] > 0`. For stage 19: `prev.id = 18`, requires `bestWaves[18] > 0`. Lock message for ascended biome: "Defeat the Wraith Father to ascend." Custom message confirmed in wg-game.js:1211. |

**Worker verdict: CONDITIONAL PASS** *(§B marker uncommitted; §C two hardcoded mechanics values — non-blocking)*

---

### 6. W-Tower-Art-Procedural

| § | Item | Verdict | Evidence |
|---|------|---------|----------|
| A | Syntax | **PASS** | node --check clean: hunt-tower-render.js, hunt-render.js |
| B | Marker integrity | **FAIL** | Done marker UNTRACKED. |
| C | Tunable discipline | N/A | Floor-tier color thresholds are rendering constants (not gameplay mechanics). |
| D | Performance | **PASS** | hunt-tower-render.js: no own rAF — `paintTowerInterior()` is a pure draw function called by main loop's `_drawFrameTower()`. No setInterval, no listeners. hunt-render.js `_drawFrameTower()` shares `_lastFrameMs`, `_trauma`, `_shakeLastMs` with `drawFrame()` — correct since they're mutually exclusive render paths. drawTowerHud() references `WG.Buffs.list()` and is guarded (`if (!p) return`). |
| E | Build-on | **PASS** | hunt-tower-render.js (new), hunt-render.js (modified: `_drawFrameTower` + `drawTowerHud` + routing in `drawFrame()`). Hunt core files UNTOUCHED. `drawFrame()` routing change: `if (!runtime) return;` → new: `if (runtime.mode === 'tower') { _drawFrameTower(); return; }` → existing `if (!runtime.stage) return;` preserved — correct. |
| F | Failure modes | **PASS** | `if (window.WG.HuntTowerRender)` guard in `_drawFrameTower()` before calling painter; fallback `WG.Render.clear(ctx, '#06020c')`. `paintTowerInterior` uses `performance.now()` for `t` — always available. Tile/sigil draw loops are bounded counts (not dynamically sized). |
| G | Path A | N/A | Pure visual feature; no monetization paths. |
| H | App Store | **PASS** | Tower interior renders procedurally — no missing art assets required. |

**Worker verdict: CONDITIONAL PASS** *(§B marker uncommitted)*

---

### 7. W-i18n-Scaffolding (worker name: Loom)

| § | Item | Verdict | Evidence |
|---|------|---------|----------|
| A | Syntax | **PASS** | node --check clean: wg-i18n.js, wg-game.js |
| B | Marker integrity | **FAIL** | Done marker UNTRACKED. |
| C | Tunable discipline | N/A | Locale strings are content. |
| D | Performance | **PASS** | No rAF, no setInterval. `_refreshDOM()` is a synchronous one-pass DOM scan (two querySelectorAll calls). `init()` is async via `fetch()` — one-shot. `setLocale()` is only called on user interaction (Settings language change). |
| E | Build-on | **PASS** | wg-i18n.js (new), wg-game.js (UI string wrapping), index.html (`data-i18n` attrs + init call + `wg-i18n.js` as first module). Hunt core files UNTOUCHED. |
| F | Failure modes | **PASS** | `init()` catches fetch errors; falls back to `en` if locale load fails. `t(key)` returns key on miss. All `WG.i18n ? WG.i18n.t('key') : 'HARDCODED'` patterns in wg-game.js ensure no crash if module absent. `setLocale()` early-return if already current locale. |
| G | Path A | N/A | |
| H | App Store | **PASS** | Locale picker shows English (active) + Spanish/Japanese (disabled, "coming soon"). Live-locale switching wired via Settings. |
| I | i18n checks | **PASS** | (1) `t(key)` fallback: `if (str === undefined \|\| str === null) return key;` — confirmed at wg-i18n.js:50. (2) English UI unchanged: all wrapped strings have `WG.i18n ? ... : 'HARDCODED'` fallbacks; `en.json` values exactly match existing hardcoded strings. (3) `en.json` valid JSON: confirmed via `node -e JSON.parse`. (4) 248 keys cover all wrapped surface: nav tabs, boot screen, menu buttons + side icons (TASKS/SHOP/FEATS/SETTINGS/PASS/DAILY/7-DAYS), stage names 1-24, boss names (including echo_throne_keeper + wraith_father_echo), results screen, settings modal, achievements, missions (including EVENT tab), compliance, daily rewards, onboarding, gacha. |

**Worker verdict: CONDITIONAL PASS** *(§B marker uncommitted)*

---

## §E Build-on guarantee — cumulative check

`git log --oneline -- js/hunt/hunt-enemies.js js/hunt/hunt-bosses.js js/hunt/hunt-stage.js js/hunt/hunt-waves.js js/hunt/hunt-player.js` last committed entries are pre-0.27.0 batch. The uncommitted 0.27.0 changes to hunt-enemies.js, hunt-bosses.js, hunt-stage.js (W-Content-Expansion) are exclusively additive — confirmed via full git diff review.

**§E cumulative: PASS (with note)** — stages 1-18 + Day/Night + combat AI + enemies (original 11 types) + bosses 1-6 UNTOUCHED. New content (stages 19-24, sigil_drone, memory_husk, echo_throne_keeper, wraith_father_echo) is additive only.

---

## §G Path A discipline — batch-level summary

| Check | Status | Evidence |
|-------|--------|----------|
| Whale gate $99.99 `mega_bundle` | **PASS** | meta-iap.js untouched by 0.27.0 workers |
| Gacha rates in privacy.html + terms.html | **PASS** | Not modified by 0.27.0 batch |
| Ysabel `rift_guests` pool locked | **PASS** | meta-gacha.js untouched by 0.27.0 batch |
| Ascended content locked behind stage 18 | **PASS** | `isStageUnlocked` checks `bestWaves[18] > 0` for stage 19 |
| i18n doesn't soften Path A mechanics | **PASS** | String wrapping is display-only; no monetization logic changed |

---

## §H Stage 19-24 lock progression

`isStageUnlocked(stage, allStages)` at wg-game.js:905 uses `prev = allStages.find(s => s.id === stage.id - 1)` → `best = (WG.State.get().huntProgress.bestWaves || {})[prev.id]` → `return !!(best && best > 0)`.

For stage 19: `prev.id = 18`. Requires `bestWaves[18] > 0`. Lock message: "Defeat the Wraith Father to ascend." (**PASS**)

Stage 24 override: `waveCount = 8; durationSec = 8 * waveDurationSec` (480s). Verified in hunt-stage.js. (**PASS**)

---

## §I i18n checks

| Check | Status | Evidence |
|-------|--------|----------|
| `t()` fallback to key on miss | **PASS** | wg-i18n.js:50: `if (str === undefined \|\| str === null) return key;` |
| English UI unchanged | **PASS** | All wrapped strings have `WG.i18n ? ... : 'HARDCODED'` fallbacks matching existing text |
| en.json valid JSON | **PASS** | `node -e JSON.parse` confirms valid |
| 248-key coverage | **PASS** | All keys referenced in wg-game.js verified present in en.json |

---

## §J Event scheduling

| Check | Status | Evidence |
|-------|--------|----------|
| Real server scheduling deferred to Phase 4 | **PASS** | meta-ltd-events.js:3: "Server scheduling = Phase 4 — dates are hardcoded." |
| Client-side dates functional | **PASS** | `_todayStr()` via `WG.MetaDailyReset.todayStr()` or `new Date().toISOString().slice(0,10)` |
| Events won't fire spuriously at launch | **PASS** | All 3 events have future or past dates relative to 2026-05-05. `lunar_new_year` (Feb 2026) is past — correctly returns 0 active events at launch. `wraith_moon` (Oct 2026) and `tower_anniversary` (May 2027) are future. |

---

## Open items / known gaps

### §B REQUIRED FIX (Architect action)

Workers 3-7 have all code in the working tree but nothing committed. The following untracked/modified files need a single commit before shipping:

**New files (untracked ??)**
- `js/core/wg-i18n.js`
- `js/hunt/hunt-tower-render.js`
- `js/hunt/hunt-tutorial-ext.js`
- `js/meta/meta-ltd-events.js`
- `locales/` (entire directory — en.json)
- `docs/I18N.md`
- `workers/done/W-Content-Expansion-Stages-19-24.done`
- `workers/done/W-Event-System-Scaffold.done`
- `workers/done/W-Tower-Art-Procedural.done`
- `workers/done/W-Tutorial-Content-Pass.done`
- `workers/done/W-i18n-Scaffolding.done`

**Modified files (M)**
- `STATE_OF_BUILD.md`
- `index.html`
- `js/hunt/hunt-bosses.js`
- `js/hunt/hunt-enemies.js`
- `js/hunt/hunt-render.js`
- `js/hunt/hunt-stage.js`
- `js/meta/meta-missions.js`
- `js/wg-game.js`

### Carried forward (non-blocking)

1. **flyIcon cancel-guard partial** — `meta-daily-rewards.js` flyIcon still lacks `cancelAnimationFrame` guard (hunt-results.js was fixed in db8934c; meta-daily-rewards.js was not). Fix: same pattern as hunt-results.js — add `rafId`/`cancelled` pair.

2. **Fragment HP hardcoded** — `hunt-bosses.js:146`: `fe.hp = fe.maxHp = 120`. Should be `fe.hp = fe.maxHp = td.splitFragmentHp || 120;` with `splitFragmentHp: 120` added to `echo_throne_keeper` catalog entry. §C violation.

3. **memory_husk split hardcoded** — `wg-game.js` `enemy:killed` handler: `spawn('lurker', ...)` with `i < 2`. Should be `ct.splitType` and `ct.splitCount` from enemy catalog entry (`memory_husk` entry needs `splitOnDeath: { type: 'lurker', count: 2 }`). §C violation.

4. **MutationObserver not disconnected** — `hunt-tutorial-ext.js` `_buffObs` (childList on body) persists after tower tutorial completes. No-op after completion. Fix: call `_buffObs.disconnect()` after `markDone('towerTutorial')`.

5. **`_bannerEl` stale reference** — `meta-ltd-events.js` hourly interval operates on a potentially detached `_bannerEl` between `showHuntStageList()` calls. Fix: add `if (!_bannerEl || !_bannerEl.isConnected) return;` to `_refreshBanner()`.

6. **gems/diamonds emoji collision** — `meta-missions.js` `_formatReward`: both `diamonds` and `gems` display as `💎`. Display-only cosmetic; no functional impact.

### Resolved from 0.26.0

- **forge.rareMaterials not persisted** — RESOLVED in carryover commit db8934c.
- **flyIcon cancel-guard (hunt-results.js)** — RESOLVED in db8934c. (meta-daily-rewards.js not resolved — see Gap 1 above.)

---

## §J SHIP BLOCKERS

**One Architect-action blocker:**

| Item | Type | Action |
|------|------|--------|
| Commit workers 3-7 working tree | **Architect git commit** | `git add` + commit all untracked/modified files listed in §B |

All prior 0.26.0 Architect-gated items (legal entity, AdMob IDs, StoreKit wiring, certificates, ASC record, ATT pre-prompt, crash reporting) remain outstanding and unchanged.

No code-level blockers found in this batch.

---

*Auditor: W-Audit-v027-Final one-shot Sonnet CLI subprocess | 2026-05-05T20:13Z*
